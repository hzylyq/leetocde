package SpiralTraverse

func SpiralTraverse(array [][]int) []int {
	if len(array) == 0 {
		return nil
	}

	res := make([]int, 0)

	m, n := len(array), len(array[0])
	for i := 0; i < m; i++ {
		for j := 0; j < n; j++ {

		}
	}
	return res
}
